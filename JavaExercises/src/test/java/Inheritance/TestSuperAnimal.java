package Inheritance;

public class TestSuperAnimal {
    public static void main(String args[]) {
//        Dog d=new Dog();  //by default parent and child constructor are executed //Animal; Dog
//        d.displayColor(); //black ; with super.color becomes white (the parent's color)
//        d.eat(); //eating bread (child by default); eating/... (the parent output)


        Dog d=new Dog("Elephant"); //from parent class
    }
}
